name: Keep Render Alive

on:
  schedule:
    - cron: "*/10 * * * *"  # every 10 minutes

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping /api/recommend
        run: curl -sf https://what-to-eat-admin-1.onrender.com/api/recommend || true
